import { HighlightCustomColorBindingDirective } from './highlight-custom-color-binding.directive';

describe('HighlightCustomColorBindingDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCustomColorBindingDirective();
    expect(directive).toBeTruthy();
  });
});
