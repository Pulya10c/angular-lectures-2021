import { HighlightWithCustomColorDirective } from './highlight-with-custom-color.directive';

describe('HighlightWithCustomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWithCustomColorDirective();
    expect(directive).toBeTruthy();
  });
});
